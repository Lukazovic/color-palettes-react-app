import React, { Component } from "react";

export default class Palette extends Component {
  render() {
    return (
      <div className="Palette">
        {/* navbar */}
        <div className="Palette-colors">{/* Colors Boxes */}</div>
        {/* Footer */}
      </div>
    );
  }
}
